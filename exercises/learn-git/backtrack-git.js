
// Backtracking in Git

1. ``` git add scene-5.txt ``` , ``` git commit -m "nice message" ``` // adds and commits

2. ``` git show HEAD ``` // Will show the head commit

3. ``` git add . ``` or ``` git add file1.txt file2.txt ``` // will add all files or a couple.

4. ``` git reset HEAD <filename> ``` // Resets the file in the staging area to be the same as the HEAD commit.

5. ``` git reset SHA ``` // correct mistake with SHA commit ID to before mistake.
