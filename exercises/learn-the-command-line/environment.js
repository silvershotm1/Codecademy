
// NANO as console editor

1. ``` nano hello.txt ``` // Ctrl + O to save Ctrl + X to exit

2. ``` nano ~/.bash_profile ``` then ``` echo "Hello, Jane Doe" ``` ```source ~/.bash_profile ```
  //    edit profile                        Add text to onload            reload changes

3. ``` alias pd="pwd" ``` // create alias for command line.

4. ``` export USER="Jane Doe" ``` then ``` echo $USER ``` // exports environment variable

5. ```env``` then  ``` env | grep Path``` // shows env vars and then search for only path
